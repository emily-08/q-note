<div class='wrapper'>
  <div class='genre'>
    <%= render "memos/genre_bar" %>
  </div>
  <%# 全てのメモ一覧 %>
  <div class="memo">
    <%# <% @memos.each do |memo| %>
    <%# 未 genreのname追加 %>
    <div class="genre-box">
      <span class="genre-label" >メモ一覧</span>
      <%= render partial: 'memos/memo', collection: @memos %>
    </div>
      <%# <%= memo.title %>
      <%# <%= memo.created_at %>
    <%# <% end %>
  </div>
</div>